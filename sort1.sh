#!/bin/bash
sort
# sort -n #numeric
# sort -h #human readable numreric
# sort -M #month of yeat
# sort -c #check if it's sorted
# sort -r #reverse
# sort -u #remove duplicated
# sort -k #sort on column
# sort -t # delimiter
# sort -o #output to a file
# sort -f #ignore cases
# sort -t $'\t' -k2,2 -n -r #start at column 2 and end at column 2
